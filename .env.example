# Postgres connection string (e.g. from Vercel Postgres / Neon, Supabase, or Railway).
# Use the same value locally and in production so both point at one database.
DATABASE_URL="postgresql://user:password@host:5432/dbname"
